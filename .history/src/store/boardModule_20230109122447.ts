const boardModule = {
  state: () => ({
    chapters: [
    ],
  }),
  mutations: {
    // setVisible(state, payload) {
    //   state.visible = payload;
    // },
  },
  namespaced: true,
};

export default boardModule;
